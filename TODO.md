# Work Items

All operational work items live in `.track/`. Use `/track:list` or
`/track:board` to inspect the current backlog.

See `.archeia/ROADMAP.md` for strategic direction and `docs/ONTOLOGY.md` for
the governance model behind the repo.
