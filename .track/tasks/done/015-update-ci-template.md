---
id: "015"
title: "[Implement] Update CI template for separate validate and build steps"
status: done
mode: implement
priority: medium
type: debt
scopes: []
project: ephemeral-derived
cycle:
created: 2026-03-17
updated: 2026-03-17
assigned_to:
pr:
depends_on: ["013"]
---

## Context

### Problem
The CI template generated by `track init` currently runs `track validate` which includes freshness checking. With freshness removed, the CI template should run `track validate` (schema/structure only) and `track build` as separate steps.

### Cause
The generated `.github/workflows/track-ci.yml` needs to reflect the new separation of concerns: validate checks correctness, build generates ephemeral local views.

### Affected Files
- `src/generator/ci.rs` — update the CI template string

### References
- Depends on: task 013
- Contract: `ephemeral-derived-project-plan.md`

## Acceptance Criteria

- [x] Generated CI template runs `track validate` and `track build` as separate named steps
- [x] The CI template does not reference freshness checking
- [x] Generator unit tests pass with the updated template

## Verification

- Run `track init` in a temp directory, read the generated `track-ci.yml` and confirm separate steps
- Run `cargo test` — all green

## Notes

- 2026-03-17 human: Seeded from ephemeral-derived-project-plan.md.
- 2026-03-17 human: Moved to todo; project plan serves as the contract (task 012 deleted).
- 2026-03-17 codex: Implemented the CI template split, updated the checked-in workflow and root docs, and ran the required validation commands.
